<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox xmlns="http://javafx.com/javafx/11.0.1" xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.chat.control.ChatHistoryWindowControl" spacing="0"
      styleClass="dialog-card"
      stylesheets="@../css/main.css">

    <!-- 标题栏 -->
    <HBox styleClass="chat-header" spacing="12" alignment="CENTER_LEFT">
        <!-- 历史图标 -->
        <Label text="📜" style="-fx-font-size: 24px; -fx-text-fill: white; -fx-padding: 0 5 0 0;" />

        <!-- 标题 -->
        <Label fx:id="titleLabel" styleClass="header-title"
               text="历史记录" />

        <Region HBox.hgrow="ALWAYS" />

        <!-- 状态标签 -->
        <Label fx:id="statusLabel" styleClass="header-title" style="-fx-font-size: 12px; -fx-opacity: 0.9;" />
    </HBox>

    <!-- 控制区域 -->
    <HBox spacing="12" styleClass="toolbar" alignment="CENTER_LEFT">
        <Label text="每次加载:" style="-fx-text-fill: #34495e; -fx-font-weight: bold;" />
        <ComboBox fx:id="limitComboBox" prefWidth="120"
                  style="-fx-background-color: #f8fbff; -fx-border-color: #c7dbff; -fx-border-radius: 8; -fx-background-radius: 8; -fx-padding: 0 10 0 10;" />

        <Region HBox.hgrow="ALWAYS" />

        <!-- 加载按钮 -->
        <Button fx:id="loadMoreButton" text="加载历史记录"
                styleClass="primary-button"
                onAction="#loadHistoryMessages" />

        <!-- 关闭按钮 -->
        <Button fx:id="closeButton" text="关闭"
                styleClass="ghost-button"
                onAction="#closeWindow" />
    </HBox>

    <!-- 分隔线 -->
    <Separator />

    <!-- 信息区域 -->
    <HBox style="-fx-padding: 8 15; -fx-background-color: #f8f9fa; -fx-border-color: #ddd; -fx-border-width: 0 0 1 0;"
          alignment="CENTER_LEFT">
        <Label fx:id="infoLabel" styleClass="label-muted"
               text="准备加载历史记录..." />
    </HBox>

    <!-- 历史消息区域 - 使用 ListView 以支持消息气泡 -->
    <ListView fx:id="historyListView" VBox.vgrow="ALWAYS"
              styleClass="list-view"
              focusTraversable="false" />

    <!-- 底部状态栏 -->
    <HBox style="-fx-padding: 10 15; -fx-background-color: #eaf2fb;" alignment="CENTER">
        <Label text="历史记录查看器" styleClass="label-muted" />
    </HBox>

</VBox>